---
title: Ceph 软件仓库镜像使用帮助
cname: 'ceph'
---

[Ceph](https://ceph.io/) 是一个开源软件存储平台，在单个分布式计算机集群上实现对象存储。


请参考[Ceph 官方安装教程](https://docs.ceph.com/en/latest/install/get-packages/)，只需要把文档中出现的 `download.ceph.com` 替换为以下链接即可。

<CodeBlock>

```
{{http_protocol}}{{mirror}}
```

</CodeBlock>


以 Debian Buster 为例，举例如下：

<CodeBlock>

```shell
wget -q -O- 'https://download.ceph.com/keys/release.asc' | {{sudo}}gpg -o /etc/apt/keyrings/ceph.gpg --dearmor
{{sudo}}apt-add-repository 'deb [signed-by=/etc/apt/keyrings/ceph.gpg] {{http_protocol}}{{mirror}}/debian-octopus/ buster main'
{{sudo}}apt update
```

</CodeBlock>
