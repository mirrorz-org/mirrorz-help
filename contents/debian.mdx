Debian 的软件源配置文件是 `/etc/apt/sources.list`。将系统自带的该文件做个备份，将该文件替换为下面内容，即可使用 TUNA 的软件源镜像。

如果遇到无法拉取 https 源的情况，请先使用 http 源并安装：

```shell
sudo apt install apt-transport-https ca-certificates
```

再使用 TUNA 的软件源镜像。

<CodeBlock
  isHttp={true}
  menus={[
    {
      title: 'Debian 版本',
      variableName: 'release_name',
      items: [
        ['sid', 'sid'],
        ['testing', 'testing'],
        ['bullseye', 'bullseye'],
        ['buster', 'buster'],
        ['stretch', 'stretch'],
        ['jessie', 'jessie']
      ]
    }
  ]}
>

```conf
# 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释
deb {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}/ {{release_name}} main contrib non-free
# deb-src {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}/ {{release_name}} main contrib non-free
deb {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}/ {{release_name}}-updates main contrib non-free
# deb-src {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}/ {{release_name}}-updates main contrib non-free

deb {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}/ {{release_name}}-backports main contrib non-free
# deb-src {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}/ {{release_name}}-backports main contrib non-free

deb {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}-security {{release_name}}-security main contrib non-free
# deb-src {{http_protocol}}{{mirrorDomain}}{{mirrorPath}}-security {{release_name}}-security main contrib non-free
```

</CodeBlock>
